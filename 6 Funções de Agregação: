a)

SELECT SUM(*) AS total_produtos FROM produtos;

b)

SELECT produto, MAXIMUM(preco) AS produto_mais_caro FROM produtos;

c)

SELECT produto, MINIMUM(preco) AS produto_mais_barato FROM produtos;

d)

SELECT SUM(IF(quantidade > 0, preco)) AS soma_total_produtos_em_estoque FROM produtos;
