a)

DELIMITER //
CREATE FUNCTION TOTAL_VALOR(preco DECIMAL(10, 2), quantidade INT)
RETURNS DECIMAL(10, 2)
DETERMINISTIC
BEGIN
    SET total = preco * quantidade; 
    RETURN total;
END //
DELIMITER ;


b)

SELECT produto, quantidade, preco, TOTAL_VALOR(preco, quant) AS valor_total FROM produtos;
